<div class="tab-pane" id="passwords">
  <table class="<%= table_css_classes %>">
    <thead>
      <tr>
        <th colspan="2"><%= _('Managed Passwords') %></th>
      </tr>
    </thead>
    <tbody>
<%- @host.passwordstate_passwords.each do |pw| -%>
      <tr>
        <td title="<%= pw.notes || pw.description.split('|').first.strip %>"><%= pw.user_name %></td>
<%- if pw.expiry_date -%>
        <td colspan="2">Expires on <%= pw.expiry_date.strftime '%F' %></td>
<%- end -%>
      </tr>
<%- end -%>
    </tbody>
  </table>
</div>
